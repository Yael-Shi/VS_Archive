# VS-Archive AI Context

VS-Archive is a Django backend project for managing historical family documents.

## Main domain

- **`ArchiveItem`** is the long-term central archival content entity. The product archive is **`/archive/`** (unified list/detail direction), not separate mini-sites per item type.
- **`OCR_DOCUMENT`:** **`Document`** remains runtime source of truth for OCR/processing fields. For the six shared archival fields, **`ArchiveItem`** is canonical on create (`create_ocr_document`), staff OCR metadata edit (`/archive/manage/<id>/edit/`), and user-facing display; **`Document`** holds compatibility mirrors. **`Document`**-centric list/review paths remain for OCR operations (`/api/ui/documents/`, `/api/ui/admin/review/`). Primary OCR create/upload UI is **`/archive/manage/new/?item_type=ocr_document`** using **`/api/uploads/*`**; **`/api/ui/upload/`** remains a fallback. Do not assume sync from Django Admin **`Document`** edits. See **`docs/ai-context/ocr-archiveitem-cutover.md`**.
- **`MANUAL_TEXT`:** **`ArchiveItem`** + **`ManualTextContent`** are runtime source of truth. Staff/admin create via **`/archive/manage/new/`** (manual-text branch; legacy alias **`/archive/manage/new/manual-text/`**) and edit via **`/archive/manage/<id>/edit/`**. No **`Document`**, no OCR/HTR, no SQS. Body is **not** in **`DocumentTextResult`**.
- **`ArchiveItem.visibility`** is the access-control source of truth for all **`ArchiveItem`** types (`public` / `private`). **`private`** means private family archive content (visible to authenticated **`archive_family`** group members and staff/admin), not staff-only content. Helpers in **`archive_item_access.py`**. **`Document.visibility`** remains a temporary bridge field; OCR document list/detail access uses **`document.archive_item.visibility`**. Family invitation/account-management is deferred.
- **Public `/archive/` search/filter:** full-text ``q`` remains authoritative for ranking. Structured advanced filters (`author` as one **`Author.id`**, repeatable `category`/`event`/`tag`/`person` ids, `year`/`year_to` overlap) compose on the same authorized queryset via ``archive_advanced_search.py``. Repeatable ``person=<person_id>`` is ArchiveItemPerson **or** identified renderable PhotoPerson (OR within Person, AND across groups); ``people_present`` does not match. List results remain one ArchiveItem; PHOTO cards may deep-link to ``?photo=`` and use that photo’s thumbnail when a single renderable PhotoContent truthfully represents the selected Person set (one selected Person: earliest matching PhotoPerson appearance, including AIP+PP overlap; several selected People: only a photo containing **all** selected ids; otherwise the ordinary item URL/primary thumbnail). Active Person filter chips are **one per selected Person id**: canonical name → ``archive-person-detail``; visible × removes only that id (preserves other filters/`q`/`item_type`/`per_page`, drops `page`, always keeps ``advanced=1``). Other chips stay grouped. PR2 adds the public advanced-search panel (`advanced=1`), active-filter chips, server-side reverse/malformed year validation, and conditional choice-context loading (not on every ordinary/q-only request; Person and Author choices follow the same rule). **PR3 completes the planned search chain** with a compact shared-nav header form (`partials/nav.html`) that submits q-only GET to ``archive-list`` (fresh `/archive/?q=…`; no advanced/current query state carried; nav input stays empty by default). No second search backend/view. Advanced search remains on `/archive/`. Public ``q`` author text and snippet match-source ``נמצא במחבר/ת`` use ordered **`ArchiveItemAuthor`** names when any structured links exist, else trimmed **`author_name`**. The advanced **author** filter is single-select **`Author.id`** via **`ArchiveItemAuthor`** (legacy **`author_name`** only for zero-link items when that **`Author.name`** is globally unique **and** the Author already has structured membership in the authorized queryset being filtered). Deploy of this Author ``q`` cutover **requires** **`backfill_archive_search_index`** before the new metadata contract is live (stale structured-item **`author_name`** tokens must stop matching; structured **`Author.name`** tokens must match). Broader ideas (Hebrew morphology, fuzzy OCR / pg_trgm, phrase search, places authority, `related`) remain separate future work.
- **`PHOTO`:** **`ArchiveItem`** is the umbrella record; a PHOTO item may own **1..N** **`PhotoContent`** rows (FK `related_name="photo_contents"`). Staff create via **`/archive/manage/new/?item_type=photo`**. Staff can add/edit/reorder/delete individual photos on the PHOTO manage page; a PHOTO item must keep at least one `PhotoContent`. Per-photo metadata (including **`PhotoPerson`**) is edited in independent cards on **`/archive/manage/<id>/edit/`**. Staff add-photo can select existing people and comma-separated **`new_person_name`** as **`PhotoPerson`** on the new photo only (not **`ArchiveItemPerson`**). Successful staff shared-metadata save stays on **`/archive/manage/<id>/edit/`**. Successful **inline** per-photo save returns to that same item edit URL with `#photo-<photo_id>`. The standalone **`/archive/manage/<id>/photos/<photo_id>/edit/`** URL remains a compatibility fallback (success still stays on that URL). PHOTO create still redirects to **`/archive/manage/`**. Staff item-manage **חזרה לפריט** goes to **`/archive/<id>/`**; each photo card (and the fallback editor) has **צפייה** to **`/archive/<id>/?photo=<photo_id>`**. Public detail presents all renderable photos on **`/archive/<id>/`** (`?photo=<photo_content_id>` selects one; invalid/foreign ids fall back to the first renderable photo). Browse cards and browse eligibility still use the **first** photo (`ArchiveItem.primary_photo_content`). Public `q` search aggregates descriptive fields and `PhotoPerson` `Person.name` / `PersonAlias.name` values from **public-renderable** `PhotoContent` rows onto the one ArchiveItem index row (no per-photo result, no `?photo=` deep-link; pending/failed/empty-key photos are omitted). Includes direct S3 upload, metadata, **`public`** / **`private`** visibility, and staff metadata edit/delete. **No** **`Document`**, OCR/HTR, or SQS. Browse-card aggregation remains **not implemented**. Thumbnail generation is implemented; S3 cleanup on delete is best-effort; re-upload/retry remain deferred. Design/scope: **`docs/ai-context/photo-archive-items.md`**. **`PhotoContent.people_present`** remains free-text and is indexed separately from identified Person names; public detail shows identified **`PhotoPerson`** canonical names for the selected photo only as links to `/archive/people/<Person.id>/` (see Person identity below).
- **Person identity:** **`Person`** is a minimal identified-person entity (canonical **`name`**, not unique; optional staff-authored public **`biography`**; plus timestamps). **`PersonAlias`** stores alternate lookup/search names for that same identity (`unique(person, name)`; not globally unique; not a display name). **`ArchiveItemPerson`** links a person as generally related to an **`ArchiveItem`**. **`PhotoPerson`** links an identified person as appearing in a specific **`PhotoContent`**. **`Author.person`** is an optional explicit FK from a bibliographic **`Author`** to one **`Person`** (`SET_NULL`; related_name **`author_identities`**; not unique — several Authors may share one Person). Staff set or clear that link from Author edit by **`Person.id`**; exact-name equality is never used to auto-link. Public People/Author unification is implemented (PR2): one public directory at **`/archive/people/`**. Staff ArchiveItem **create and edit** are the production write paths for **`ArchiveItemPerson`** on every item type (canonical-name-only new Person; **`new_archive_item_person_name`** may be comma-separated; exact case-insensitive canonical/alias matches warn and require per-token force-create rather than silent reuse or merge; aliases are picker recognition labels only). PHOTO create still means item-level related-to, not photo appearance (no **`PhotoPerson`** on create). User-submitted ArchiveItemPerson suggestions are **explicit ADD/REMOVE deltas** (`ArchiveItemPersonSuggestion`, identity=`Person.id`; stale approval is APPROVED no-op; never `set_archive_item_people`). **C2a** is the model + submit/apply services. **C2b** adds the public metadata-suggestion Person delta UI and a dedicated staff Person-suggestion backlog (identity remains `Person.id`; no new-Person proposals; PhotoPerson stays separate; historical person Tags unchanged on that form). New-Person proposals are out of scope. Staff PHOTO item-edit cards, the standalone photo editor, **and add-photo** can select existing people and create name-only **`Person`** rows as **`PhotoPerson`** (`new_person_name` may be comma-separated; exact canonical/alias matches warn and require per-token force-create; no automatic reuse or merge). The staff picker shows canonical name plus aliases (`Canonical (Alias 1, Alias 2)`); selected people have an **עריכת אדם** link to `/archive/manage/people/<id>/edit/` for canonical rename, optional public biography, and alias add/edit/delete (PR6a services). Staff can find existing Person rows at **`/archive/manage/people/`** (GET, optional `q` on canonical name and aliases; `ArchiveItemPerson` and `PhotoPerson` counts stay separate). Public catalog is **`/archive/people/`** (`archive-people-index`): Person-backed rows (AIP, renderable PhotoPerson, **or** linked Author with public AIA) plus Author-only rows (public AIA and no `Author.person`); linked Authors are not separate rows; no name-based merge; order display name then kind then id; 48/page; optional `q` on Person name/alias, linked Author names that have authorized+browse-renderable AIA for the current user, and unlinked Author names; one DISTINCT ArchiveItem count per identity. Staff can merge a duplicate **`Person`** INTO the current row from **`/archive/manage/people/<keeper_id>/edit/`** using an explicit duplicate **`Person.id`** (confirmation GET at **`/archive/manage/people/<keeper_id>/merge/`**; only confirm POST executes; service in **`person_merge.py`**). Frozen historical Person ids from **`HISTORICAL_PERSON_NAME_TAG_RECORDS`** may be keepers but must never be deleted as the duplicate. Biography copies only when keeper is empty; different nonempty biographies fail closed. Aliases move/dedupe; duplicate canonical name can become a keeper alias. **`ArchiveItemPerson`** and **`PhotoPerson`** move independently with collision dedupe and no cross-inference. Authors explicitly linked to the duplicate Person are **repointed** to the keeper (not inferred by name). Pending **`ArchiveItemPersonSuggestion`** uniqueness conflicts fail closed; reviewed suggestions are repointed. **`ReviewedPersonImportBinding`** rows on the duplicate are repointed to the keeper (Person FK is PROTECT). Person and Author stay separate. There is no Person Admin. Reviewed PhotoPerson import is operator-only: **`import_reviewed_photo_people`** (dry-run default, **`--apply`**) reads `photo-person-reviewed-import-v1` JSON; **`create_person`** idempotency is the binding table, never name match. **`people_present`** is not parsed or overwritten. See **`decision-log.md`**. Public PHOTO detail shows canonical **`Person.name`** for the selected photo only as links to `/archive/people/<Person.id>/`; aliases are not publicly displayed. Public Person detail is **`/archive/people/<id>/`** (canonical name; optional staff-authored plain-text **`biography`** when nonempty, unlabeled, autoescaped, line-breaks preserved). The page is accessible when the user can see at least one authorized/renderable **`ArchiveItemPerson`** item, authorized/renderable **`PhotoPerson`** appearance, **or** **`ArchiveItemAuthor`** item via **`Author.person`**; 404 when none is visible. Related holdings are one unified ArchiveItem list (`פריטים קשורים`; DISTINCT AIP ∪ PhotoPerson ∪ linked-Author AIA; `-created_at`, `pk`; 48/page). PhotoPerson matches deep-link to the earliest matching renderable photo (`/archive/<id>/?photo=<photo_content_id>`); AIP-only items use the normal item URL. Biography is display-only on an otherwise accessible Person page and is **not** indexed. Public cards and detail show item-level **`ArchiveItemPerson`** under **אנשים קשורים** (links to `/archive/people/<Person.id>/`); the 29 frozen historical person-name Tag ids are hidden from public card `קשור ל־` and detail `תגיות:`. PhotoPerson **אנשים מזוהים:** on PHOTO detail stays separate. Stage B hides the 29 mapped Tag ids from public advanced Tag choices and 302-redirects `/archive/tags/<mapped_tag_id>/` to `/archive/people/<mapped_person_id>/` (ID map only, before `Tag.objects.get`). Direct legacy `?tag=` query application is unchanged. Public `q` indexes **`ArchiveItemPerson`** canonical names and aliases on the owning ArchiveItem for every item type (item metadata, not photo appearance; no `?photo=` / Person deep-link). **`Person.biography` is not searchable.** Public advanced search filters by repeatable **`person=<person_id>`** through **`ArchiveItemPerson` or identified renderable `PhotoPerson`** (canonical name in the picker; one active chip per selected id, name → Person page, × removes that id and keeps `advanced=1`; aliases are not separate choices; `people_present` does not match). Membership is unchanged (OR within Person). PHOTO result **cards** may deep-link to the earliest truthful matching renderable photo (`?photo=`) as on Person detail; several selected People deep-link only when one photo contains all of them. `PhotoPerson` canonical names **and** aliases remain searchable on the owning PHOTO ArchiveItem only when that Person is attached to a public-renderable photo. D1
(`cleanup_historical_person_tags --apply-relations`) deletes mapped
`ArchiveItem.tags` and `Document.tags_m2m` through rows and rebuilds
search indexes for affected ArchiveItems; Person / ArchiveItemPerson /
PhotoPerson / ordinary Tags are untouched. Mapped ids are not public Tag
choices and mapped Tag browse URLs redirect. Direct `?tag=<mapped_id>`
still uses Tag-relation semantics (matches nothing after D1). D2a
retired historical Tag-name policy is implemented (exact names stay
blocked even if the mapped Tag row is gone). D2b
(`delete_historical_person_tag_rows --apply-rows`) deletes the 29 frozen
Tag rows when all 29 exact `(id, name)` rows are present, mapped through
relations are 0, and the retired-name PENDING inventory is empty. The
frozen map remains. After D2b, mapped Tag rows are absent; D1 and
reconciliation treat that as `planned=0`.
No automatic aliases. `Person.name` is not Tag identity. Migration **`0055_backfill_person_from_person_name_tags`** copied 29 approved Tags into `Person` + item-level `ArchiveItemPerson` only; `PhotoPerson` and aliases were not inferred. Because `0055` predated ArchiveItemPerson search indexing, deploy of that search change requires **`backfill_archive_search_index`**. Alias create/edit/delete and `update_person_name` fan out to ArchiveItems linked through ArchiveItemPerson **and** PhotoPerson (one rebuild per item). Tags and `author_name` are unchanged. See **`decision-log.md`**.
- **Author (bibliographic):** **`Author`** is separate from **`Person`** (name not unique; titles such as ד״ר / Dr. stay in the stored name; no title-stripping or spelling merge). Optional **`Author.person`** is an explicit staff-only link to at most one **`Person`** (nullable FK, not OneToOne; never name-inferred). **`ArchiveItemAuthor`** is the ordered through row for **`ArchiveItem.authors`** (`position` from 0; unique `(archive_item, author)` and `(archive_item, position)`). **`ArchiveItem.author_name`** remains the compatibility field and public display **fallback** when an item has no **`ArchiveItemAuthor`** rows. Public **`q`**, snippet author match-source, and the advanced Author filter use structured links when present (stale **`author_name`** is not searchable/filterable then). Public browse cards and non-PHOTO source metadata render structured links in **`position`** order to **`/archive/people/<person_id>/`** when **`Author.person_id`** is set, otherwise **`/archive/authors/<author_id>/`** (duplicate names stay distinct; links win over drifted **`author_name`**; **`author_name` is never parsed to infer Authors or Persons**). PHOTO detail still omits Author/source metadata; PHOTO cards use the same rule on the existing author line. Backfill is 1:1 on exact non-empty `author_name` (no comma split; empty/whitespace skipped; every item type with a stored value, including PHOTO). Staff create/edit for **OCR_DOCUMENT**, **MANUAL_TEXT**, and **VIDEO** uses **`author_ids`** plus comma-separated **`new_author_name`** (`apply_staff_archive_item_authors`); a unique exact typed name is reused, unmatched tokens create, and multiple exact matches fail closed. Linked authors use keep/remove checkboxes (unlink does not delete **`Author`** rows). Dual-write is `author_name = ", ".join(ordered names)` (joined string capped at 255). The service **`author_name=`** path still uses **`apply_legacy_author_name`** (no comma split; reuse one exact match or create; fail closed on duplicate exact names). PHOTO author UI is still excluded and PHOTO writers do not dual-write. Staff can list Authors at **`/archive/manage/authors/`** (optional `q` on **`Author.name`** only; **`ArchiveItemAuthor`** counts; edit links to **`/archive/manage/authors/<id>/edit/`**). Staff can merge a duplicate **`Author`** INTO the current row from **`/archive/manage/authors/<keeper_id>/edit/`** using an explicit duplicate **`Author.id`** (confirmation GET at **`/archive/manage/authors/<keeper_id>/merge/`**; only confirm POST executes; service in **`author_merge.py`**). Same-item collisions drop the duplicate link and keep the keeper slot (not first-occurrence-wins); positions compact to `0..n-1`; **`author_name`** is rebuilt from final ordered links; search-index fan-out is duplicate-linked items only, in the same transaction. Duplicate name is not kept as an alias. If exactly one Author is explicitly linked to a Person, the keeper retains that Person; both linked to the same Person is allowed; both linked to different Persons fails closed before writes. Staff Author edit can set or clear **`Author.person`** by Person id without creating Person rows. Public **`/archive/authors/`** is a compatibility redirect to **`/archive/people/`** (preserves `q`). Unlinked Authors with public AIA remain Author-only rows on that People directory. Linked Author detail redirects to **`/archive/people/<person_id>/`** only when that Author has authorized+browse-renderable AIA; otherwise 404 (does not leak `Author.person`). Unlinked public Author detail remains **`/archive/authors/<id>/`** (H1 name; **פריטים קשורים**; shared browse cards; normal item URLs; `-created_at`, `pk`; 48/page; 404 when no visible linked item). There is no standalone Author delete. One-time reviewed cleanup **`cleanup_legacy_comma_authors`** (dry-run default; **`--apply`**) rewrites only the audited ArchiveItem **311** mapping and deletes four unlinked aggregate Authors; it is **not** a general comma-split. See **`decision-log.md`**.
- **`VIDEO`:** **`ArchiveItem`** + **`VideoContent`** are runtime source of truth for URL/provider presentation metadata only (no media bytes/S3). Staff create/edit/delete via **`/archive/manage/`**. Public browse/detail support YouTube click-to-load (`youtube-nocookie.com` after explicit activation; iframe `referrerPolicy=strict-origin-when-cross-origin` to avoid YouTube error 153; local facade replaced in-place by the player in one media box — 16:9 by default with `min-height: 200px`, so narrow mobile may be slightly taller than 16:9) and KAN/OTHER external links. Site-wide Referrer-Policy remains `same-origin`; CSP `frame-src` allows only `'self'` and `https://www.youtube-nocookie.com`. Public render revalidates stored URL/provider/mode/id via `parse_video_url()`. Metadata-only search; type filter “סרטונים”. No captions/transcription/OCR. See decision-log VIDEO PR1–PR3 and the error-153 / unified-facade follow-up.
- Documents may be uploaded as IMAGE or PDF.
- Documents have metadata (`language`, `text_input_type`, etc.).
- OCR/HTR extracts text into `DocumentTextResult` rows.
- After OCR/HTR, Hebrew documents use the transcript for **`HEBREW_TEXT`**; non-Hebrew documents persist **`SOURCE_TEXT`**, then automatic Gemini Hebrew translation to **`HEBREW_TEXT`** (see **`docs/ocr-routing-reference.md`**).
- Text results are stored separately from the document.
- Admin review/verification matters (`verification_status` on results).

## Current implementation (OCR/HTR)

**Gemini** is the **production/default** engine for all routed pairs except Hebrew handwritten. Static `OCR_ROUTES` in `documents/services/ocr_routing.py` remain Gemini-only for the pairs that still use Gemini.

**Transkribus** is **implemented** (Legacy TrpServer / PyLaia), but **not** broad production-default:

- Real adapter + engine: `TranskribusAdapter`, `transkribus_engine.py`.
- Worker routing: `language=he` + `HANDWRITTEN` requires Transkribus. `ENABLE_TRANSKRIBUS_HEBREW_HANDWRITTEN=true` activates that route; if the flag is off, routing fails fast instead of using Gemini.
- Existing `TRANSKRIBUS_DEV_UPLOAD_MODE`, `TRANSKRIBUS_USE_EXISTING_SERVER_DOCUMENT`, `TRANSKRIBUS_FORCE_REPROCESS`, and `TRANSKRIBUS_RECOGNITION_ONLY_RETRY` remain execution/recovery controls, not route-selection flags.
- Manual smoke: `python manage.py dev_transkribus_transcribe <file> --confirm-create-transkribus-doc`.

**Not implemented:** Gemini→Transkribus fallback, Transkribus→Gemini fallback, hybrid OCR routing, broader Transkribus routing beyond Hebrew handwritten, product/admin reprocess workflow, cleanup automation, general re-OCR on successful Trp runs, remote Trp deletion, or automatic paragraph inheritance.

**Durable `PROCESS_DOCUMENT` rollout:** Request model, typed worker outcomes, request-aware worker fencing, and the generic enqueue service are implemented. Successful single-file `upload_complete` and multi-image `upload_finalize` now use `enqueue_uploaded_document_processing` with `origin=UPLOAD_FINALIZE`; repeated completion coalesces against active history and treats matching terminal upload history as idempotent. Document-state updates are fenced by Request status, and expected enqueue failures use safe typed responses. The ECS task role has explicit SQS send permission. OCR reprocess and Hebrew translation retry remain on the legacy document-id payload; stranded pre-send `QUEUED` recovery remains deferred.

**Corrected/current staff sync queue (PR1 schema + PR2 worker + PR3 enqueue service + staff fetch UI):** durable **`TranskribusCorrectedCurrentSyncRequest`** per document enqueue intent; top-level SQS type **`SYNC_TRANSKRIBUS_CORRECTED_CURRENT`** handled by the existing worker (claim/reclaim, 45m lease + visibility, 2m defer, atomic Request↔Attempt link before provider I/O, idempotent terminal reconciliation). Linked stale **`STARTED`** (≥60m) → **`RECOVERY_REQUIRED`** (keeps **`lease_token`**, clears **`lease_expires_at`**; no provider rerun; late fenced worker may still terminalize). Guarantee: at-most-once provider orchestration per Request, not exactly-once delivery. **PR3** adds `enqueue_transkribus_corrected_current_sync` (Document-lock send-right; post-send CAS; no `QUEUED`/`RUNNING`/`RECOVERY_REQUIRED` resend; stranded pre-send `QUEUED` deferred to recovery/requeue). **Staff UI POST** on the existing corrected/current attempts page enqueues via that service only (no sync in the web process; no automatic activation). Feature gate / recovery command remain deferred. Management command **`sync_transkribus_corrected_current`** unchanged (no Request correlation).

**Recognition-only retry V1 (dev/staging):** when `TRANSKRIBUS_RECOGNITION_ONLY_RETRY=true`, dev upload mode may re-run PyLaia on an existing Trp `remote_doc_id` without a new upload — **recovery only** (failed/incomplete upload-created attempts). Excludes `SUCCEEDED` source runs; blocks if any `DocumentTextResult` is `VERIFIED`. `TRANSKRIBUS_FORCE_REPROCESS=true` still means a new upload/new Trp document. See `decision-log.md`.

**TranskribusRun persistence:** Transkribus adapter paths persist one row per attempt (remote ids, job ids, attempt status). Worker passes generic `document_id` only.

**Cleanup / retention V1:** `python manage.py report_transkribus_cleanup` is a dry-run local reporting command. It classifies `TranskribusRun` rows and grouped `remote_doc_id` lineages for operator review, but it does not call Transkribus, delete remote docs, or delete local rows.

**Duplicate upload guard (PR3):** dev upload mode blocks a second Trp upload for the same `(document_id, UPLOAD_CREATED, collection_id, model_id)` when a prior blocking run exists. Override with `TRANSKRIBUS_FORCE_REPROCESS=true` (new Trp doc) or recovery with `TRANSKRIBUS_RECOGNITION_ONLY_RETRY=true` when a reusable source run exists. See `decision-log.md`.

**Transkribus paragraph presentation (PR1 metadata + PR2 public rendering + PR3 staff editor/status):** v1 is Transkribus-only. Staff-authored paragraph boundaries are presentation metadata on a `TranskribusTranscriptSnapshot` (`TranskribusParagraphMapping` + `TranskribusParagraphBreak` after contributing source lines). They do **not** modify `DocumentTextResult.text`, snapshot canonical text, snapshot lines, geometry, char offsets, hover IDs, or search offsets. A mapping row with zero breaks is an explicit one-paragraph save; no mapping row means grouping was never saved. New snapshots do not inherit mappings; historical mappings remain stored. Currentness follows binding structural freshness + same snapshot (not `hover_eligible`). **Public rendering (PR2):** when `assess_paragraph_mapping_currentness` reports a current mapping, the public document detail transcription is wrapped as human `<p>` groups over existing hover/search fragments (A-style overlay). No/stale/other-snapshot/drifted mappings keep the legacy renderer unchanged. Canonical separators (`\n`, `\n\n`) stay in the DOM; CSS collapses them visually inside a paragraph while source-line spans keep `pre-wrap`. A page boundary is not a paragraph boundary. Gemini, manual text, photo, and video paths are unchanged. **Staff editor/status (PR3):** authorized staff edit paragraph breaks on `ui/admin/documents/<id>/transkribus-paragraphs/` against the current displayed/bound Transkribus snapshot, in original source-line order (not flowed prose). Save uses `save_paragraph_mapping` (POST/PRG, CSRF, `_require_admin_page` + `get_viewable_document`). Zero-break save is an explicit one-paragraph mapping. A stale-submit token (`expected_document_id`, `expected_text_result_id`, `expected_snapshot_id`) refuses writes if the bound snapshot or structural freshness changed. Status appears on the Transkribus versions page (“תעתוק Transkribus המוצג כעת”) and as a compact staff-only note on document detail; it reflects the current snapshot only. **Historical suggestion/adoption (PR4):** when the editor’s current bound snapshot is structurally fresh and has no mapping, eligible older mappings may be offered as optional suggestions (PR1 correspondence proof). Multiple eligible sources are listed newest-first for explicit staff choice; none is auto-adopted. Adoption is a dedicated POST (`.../transkribus-paragraphs/adopt/`) that creates a new current mapping with `copied_from` and refuses if a current mapping already exists. Versions/detail do not run historical discovery or show adopt actions. Cross-snapshot transfer remains a fail-closed `(page_index, provider_line_id)` identity proof.

## Routing layer (done)

Engine selection is implemented as a small routing layer (`select_ocr_route` → `transcribe_pages` → adapters), not as logic inside views or a provider-specific worker.

## OCR review lifecycle (current)

**Automatic OCR/HTR success** (`run_worker._save_htr_results`):

- **`DocumentTextResult.status=NEEDS_REVIEW`** (Gemini, Transkribus, and any worker success path)
- **`verification_status=UNVERIFIED`**
- **`NEEDS_REVIEW`** = usable/displayable text that still needs human review; **not** a technical failure
- **`FAILED`** = pipeline/dispatch/routing failures only
- **Write-time VERIFIED fence:** if any `DocumentTextResult` for the document is **`VERIFIED`** at successful automated persist time, skip creating/updating automated SOURCE/HEBREW rows and do not replace the search-index body with that late result. Restore the pre-run `processing_state_user` (do not roll up from the unused runtime engine or from one VERIFIED row’s engine). `REJECTED` does not fence. Ordinary `UNVERIFIED` reruns and cross-engine writes remain allowed when no row is `VERIFIED`.

**Human ground truth:** **`verification_status=VERIFIED`** is the human-approved layer. Row **`status`** does not replace that.

**Public text quality:** persisted **`DocumentTextResult.quality`** is **`UNKNOWN`/`LOW`/`MEDIUM`/`GOOD`** (default **`UNKNOWN`**; no historical inference). Effective public quality: **`VERIFIED` → `HUMAN_VERIFIED`**, **`REJECTED` → `NEEDS_CORRECTION`**, else the persisted base value (`documents.services.text_quality`). **`HUMAN_VERIFIED` and `NEEDS_CORRECTION` are not stored.** Automatic **LOW/MEDIUM/GOOD** is currently scored only for **Arabic printed banded SOURCE_TEXT** from completed `page_quality` values plus assembled text (missing/blank assembled text stays **UNKNOWN**; `[UNCLEAR]` caps all-UNASSISTED **GOOD** to **MEDIUM**; ≥ half `CLOUD_VISION_LOW_QUALITY` is **LOW**; `ASSISTED`/`MIXED` alone are **MEDIUM**). Gemini, Transkribus, non-banded Antigravity, and other OCR routes remain **UNKNOWN**. The worker writes SOURCE_TEXT `quality` only when `HtrResult` explicitly supplies a persisted value. Successful Gemini Hebrew translations of non-Hebrew OCR persist **`HEBREW_TEXT.quality`** as the inherited same-engine SOURCE_TEXT base quality (`capped_inherited_base_quality`; no independent translation score; verification_status is not used). Missing SOURCE fails closed to **UNKNOWN**. Hebrew-native / Transkribus **`HEBREW_TEXT`** is unchanged (OCR mirror; does not copy `HtrResult.quality`). Failed translations do not inherit. No historical backfill. Staff **`MANUAL_TEXT`** is **`HUMAN_VERIFIED`** in that helper only. Public OCR detail shows **one** quality indicator on the **displayed transcription** (`text_quality_presentation`): SOURCE_TEXT for non-Hebrew documents; for Hebrew-language documents, the existing single-panel transcription choice (HEBREW_TEXT preferred, else SOURCE_TEXT). The translation panel has **no** separate public badge. The tooltip’s translation-dependency sentence appears **only when a Hebrew translation is displayed alongside transcription**. `MANUAL_TEXT` detail uses the same shared indicator without that sentence. Engine-marker identity is unchanged.

**Parent document rollup:**

- **`READY`** = all expected outputs exist and are usable/displayable (non-empty text; `SUCCEEDED` or `NEEDS_REVIEW`). **Not** human-verified.
- A document may be **`READY`** while results are **`NEEDS_REVIEW`** + **`UNVERIFIED`**.
- **`PARTIAL`** = missing, incomplete, failed, or unusable expected outputs — **not** merely review pending.
- **`NEEDS_REVIEW` alone does not force `PARTIAL`** when expected usable rows exist.

**Review reasons:** policy token **`AUTOMATIC_OCR_REQUIRES_HUMAN_REVIEW`** on every automatic success; **`NEEDS_REVIEW_FLAG`** only when **`HtrResult.needs_review=True`**; plus **`MIN_TEXT_LENGTH`**, **`HAS_UNCLEAR`**, and engine-provided reasons.

**`SUCCEEDED`** remains valid in the schema for future trusted paths; current automatic worker persistence normally uses **`NEEDS_REVIEW`**.

## Hebrew result types (current behavior)

For Hebrew documents, the worker persists **both** `SOURCE_TEXT` and `HEBREW_TEXT`; processing-state rollup for **`READY`** requires **`HEBREW_TEXT` only** (usable per rollup rules above). See `decision-log.md` and `.cursor/rules/architecture.mdc`.

## Non-Hebrew `PARTIAL`

Non-Hebrew documents may remain **`PARTIAL`** when **`HEBREW_TEXT`** is missing or translation failed — **intentional**, not an OCR failure. **`READY`** rollup requires usable **`SOURCE_TEXT`** and **`HEBREW_TEXT`**.

## Where to read more

- `docs/ocr-routing-reference.md` — current OCR/HTR routing, models, and translation behavior.
- `docs/ai-context/decision-log.md` — durable decisions, Transkribus PR history, operational boundaries.
- `docs/ai-context/photo-archive-items.md` — PHOTO item design/scope (V1 implemented; deferred follow-ups documented there).
- `.cursor/rules/architecture.mdc` — layer boundaries and contracts for code changes.

## Near-term roadmap

1. ~~Transkribus remote identity schema + persistence wiring~~ → **done (PR1 + PR2)**.
2. ~~Duplicate upload guard (dev upload mode)~~ → **done (PR3)**.
3. ~~Recognition-only retry V1~~ → **done** (dev/staging recovery).
4. ~~Cleanup / retention V1 dry-run reporting~~ → **done**.
5. Remote deletion / cleanup automation later, only with explicit approval.
6. Broader Transkribus production routing only with explicit approval.

OCR quality/fidelity validation against the Transkribus UI is important but **not** the current docs/rules task.
